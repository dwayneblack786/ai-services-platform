spring:
  data:
    mongodb:
      # Overridden by flapdoodle embedded MongoDB auto-configuration in tests
      database: listing-test

  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

listing:
  vision-server-url: http://localhost:8001
  pipeline:
    review-timeout-hours: 72

langchain4j:
  anthropic:
    api-key: test-key
    model-name: claude-haiku-4-5-20251001
    max-tokens: 100
