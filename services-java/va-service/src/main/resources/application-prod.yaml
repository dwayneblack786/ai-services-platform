# Production Environment Configuration
# This file is automatically loaded when running with --spring.profiles.active=prod

api:
  endpoints:
    llm:
      url: ${LLM_ENDPOINT_URL}  # Set via environment variable
      api-key: ${LLM_API_KEY}   # Set via environment variable
    
    # Add other production endpoints here
